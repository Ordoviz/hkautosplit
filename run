#!/bin/sh

splitsfile="$1"  # .lss file as generated by hksplitmaker.com
url='ws://localhost:50000'

if [ -n "$WAYLAND_DISPLAY" ]; then
	wl-copy "$url"
else
	printf %s "$url" | xclip -selection clipboard
fi

sudo python src/main.py "$splitsfile"
